// EXPECTED_OUTPUT: Binary string operations only support '+' with two strings
#include "std/io.eth"

i32 main() {
    string s = "hi";
    i32 x = 1;
    string y = s + x;
    printf("%s\n", y);
    return 0;
}
